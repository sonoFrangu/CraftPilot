# CraftPilot per Windows

Il launcher Windows avvia agente e dashboard nello stesso processo operativo e apre una finestra app dedicata.

Per lo sviluppo: `npm run desktop:windows`.

Per preparare installer NSIS e portable: `desktop/windows/build.ps1`.
