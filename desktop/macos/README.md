# CraftPilot per macOS

Il launcher macOS usa l’agente locale, apre una finestra app dedicata e mantiene i dati in `minecraft-data/`.

Per lo sviluppo: `npm run desktop:mac`.

Per preparare il DMG: `./desktop/macos/build.sh`.
